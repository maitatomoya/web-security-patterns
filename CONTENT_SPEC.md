# CONTENT_SPEC.md — トピック教材データ仕様書

後続エージェントがトピック教材（`public/content/topicNN.js`）を作成するための仕様書です。
このファイルとサンプル実装 `public/content/topic01.js`（XSS、完成見本）を必ず両方読んでから着手してください。

## 大原則

- 回答・コメント・ドキュメントはすべて**日本語**。**絵文字禁止**。日本語に不要な半角スペースを入れない。
- 1ファイル1トピック。`registerTopic({...})`を1回だけ呼ぶ。
- ファイル名は`topicNN.js`（NNは2桁ゼロ埋め、01〜15）。ファイル内の`id`は数値（`1`〜`15`）でファイル名と一致させる。
- 対象読者は実務経験0〜3年の初心者。専門用語は初出で必ずかみ砕いて説明する。ただしミドル層にも通用する技術的な正確さを保つ。
- 攻撃手順は「防御を理解するための説明」として書く。読者が他人のサイトで試すことを助長する表現（コピペで動く完全な攻撃ツール等）は書かない。防御側の視点を常に添える。
- **セキュリティ・個人情報**: 実在の企業名・実在の脆弱性の被害者・本物のトークンやパスワードは書かない。攻撃者のサーバー例は`evil.example`、一般例は`example.com`など予約ドメインを使う。

## ファイルの雛形

```js
/**
 * トピックNN: <タイトル>
 * カテゴリ: <カテゴリ名>
 */
registerTopic({
  id: NN,
  category: "インジェクション", // 下記4カテゴリのいずれか（完全一致）
  title: "タイトル",
  summary: "一覧カードとサイドバー・検索に出る1行概要（40〜70字目安）",
  keywords: ["別名", "英語名", "関連用語"], // 検索用。任意だが推奨
  overview: "…", // HTML文字列
  attack: { scenario: "…", steps: [ { title, detail } ], note: "…" },
  codeComparison: [ { title, lang, description, vulnerable: {...}, fixed: {...} } ],
  quiz: [ { question, choices:[4], answerIndex, explanation } ], // 3問
  checklist: [ "…" ], // 5〜8項目
  references: [ { title, url, note } ],
});
```

`overview`のように複数行のHTMLは、`topic01.js`のとおり配列を`.join("")`でつなぐと書きやすい。

## 各フィールドの仕様

### id / category / title / summary
- `id`: 数値。ファイル名の番号と一致。
- `category`: 次の4つのいずれか（**文字列完全一致**。表記ゆれ厳禁）。
  - `"インジェクション"` / `"認証・認可"` / `"設定ミス"` / `"運用"`
- `title`: トピック名。`.tmp/topic-list.md`のタイトルに合わせる。
- `summary`: 1行概要。「何が起きる脆弱性か」を一文で。

### overview（概要）
- HTML文字列。**400〜800字程度**。`<p>`で段落分けする。
- 「何が起きるか」「なぜ危険か（被害の具体例）」「なぜ起こるのか（根本原因）」を必ず含める。
- 使えるタグ: `<p> <strong> <code> <ul> <li> <table>` など。`<script>`は書かない（表示上の説明で必要なタグは`&lt;`でエスケープして文字列として見せる）。
- 強調は`<strong>`、コード片・タグ名・関数名は`<code>`で囲む。

### attack（攻撃の仕組み）
- `scenario`（任意）: 導入のHTML文字列。「どんな題材で説明するか」の一文。
- `steps`（必須）: 攻撃者視点の手順。各要素は`{ title, detail }`。
  - `title`: その手順の見出し（体言止め or 短文）。
  - `detail`: HTML文字列。1〜3文。コードやペイロード例は`<code>`で囲み、タグは`&lt;`でエスケープ。
  - 目安4〜6ステップ。「入口を探す→試す→本番ペイロード→被害発生→悪用」の流れが基本形。
- `note`（任意・推奨）: 補足のHTML文字列。緩和策と根本対策の違いなどを添えると教育効果が高い。

### codeComparison（脆弱／修正コード比較）
- 配列。**1〜2個**を推奨（サーバー側とクライアント側、など観点を分ける）。
- 各要素:
  - `title`: この比較の見出し（例「サーバー側：コメントの出力」）。
  - `lang`: 言語表示ラベル（例`"JavaScript (Node.js / Express)"`, `"SQL"`, `"HTML"`）。トピックに合った言語を選ぶ。
  - `description`（任意）: この比較で何を見るかの導入文（プレーンHTML）。
  - `vulnerable` / `fixed`: それぞれ次の形。
    - `label`: ペインの見出し（省略時は「脆弱なコード」「修正したコード」）。
    - `code`: コード文字列。**改行は`\n`**。配列を`.join("\n")`でつなぐと書きやすい。行番号は自動採番される。
    - `highlights`: 強調する**行番号の配列（1始まり）**。脆弱側は問題行、修正側は修正行を指定。範囲外の番号はvalidateでエラー。
    - `note`: HTML文字列。脆弱側は「どこが問題か」、修正側は「どう直したか」を書く。
- **左右で同じ処理の before/after** にする。行がおおむね対応していると差分が読みやすい。
- コードはあくまで教材。実在の秘密情報を書かない。長すぎない（1ペイン15行以内目安）。

### quiz（4択クイズ）
- **必ず3問**。各要素:
  - `question`: 問題文。
  - `choices`: **4つ**の選択肢（文字列。先頭にABCDや番号は付けない。UIが付与する）。
  - `answerIndex`: 正解の**0始まりインデックス**（0〜3）。
  - `explanation`: 解説のHTML文字列。**なぜ正解か＋なぜ他が誤りか**を書く。40字以上。
- 正解の位置は問題ごとにばらけさせる（全部同じ位置にしない）。

### checklist（実務チェックリスト）
- **5〜8項目**。各項目は文字列（HTML可。`<code>`を使ってよい）。
- 「〜している」「〜を確認した」という**実務で確認できる粒度**にする。抽象論は避ける。

### references（参考資料）
- 公式・一次情報を優先（OWASP、MDN、RFC、各ブラウザベンダー、CWEなど）。**実在する定番URLのみ**。
- 各要素`{ title, url, note }`。`title`は日本語推奨（英語ページには`（英語）`と`note`に添える）。`url`はhttpsのみ。
- 3〜6件目安。**作成後に必ずcurl等でHTTP 200（または301/302）を確認**し、デッドリンクを載せない。

## HTMLエスケープの注意（重要）

教材本文で「`<script>`」のようなタグを**文字として見せたい**場合は、必ず`&lt;script&gt;`と書く。
生の`<script>`や`<img onerror=...>`をHTML文字列に入れると、実際にサイト上で実行されてしまい、
教材サイト自身がXSSになる。app.js側は`overview`等をそのままinnerHTMLに入れるため、ここは執筆者の責任。
（コード比較の`code`フィールドは自動エスケープされるので生タグでよい。）

## 完成後の検証手順（必須）

```bash
# 1. 構文チェック
node --check public/content/topicNN.js

# 2. スキーマ検証（該当ファイルのみ、または全件）
node scripts/validate.js public/content/topicNN.js
node scripts/validate.js

# 3. 参考リンクの死活確認（例）
curl -s -o /dev/null -w "%{http_code}\n" "https://owasp.org/..."

# 4. ローカル起動して目視（UIで表示崩れ・コード比較・クイズ正誤・進捗保存を確認）
node server.js   # http://localhost:3950 、確認後は必ず停止
```

`validate.js`でエラー0件、参考リンクが全て生存、UIで崩れなし、を満たして完了とする。

## 執筆スタイルのチェックポイント

- 見出し・段落で構造化し、初心者が上から順に読めばわかるようにする。
- 「なぜ」を必ず説明する（対策の丸暗記にさせない）。
- 攻撃と防御を必ずセットで示す。攻撃手順の直後に「これを防ぐには」を置く。
- 用語は初出でかみ砕く（例: 「CSP（Content-Security-Policy。ブラウザに読み込み元を制限させるヘッダー）」）。
